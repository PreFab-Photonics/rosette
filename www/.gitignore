# deps
/node_modules

# generated content
.source
rosette.json

# test & build
/coverage
/.next/
/out/
/build
*.tsbuildinfo

# misc
.DS_Store
*.pem
/.pnp
.pnp.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# viewer embed: www/public/viewer/ is intentionally NOT ignored (committed).
# Built from app/ via ./scripts/build_landing_viewer.sh so Vercel can deploy
# without the Rust/wasm-pack toolchain. Rebuild locally with that script.

# others
.env*.local
.vercel
next-env.d.ts